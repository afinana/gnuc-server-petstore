{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "PETSTORE-API",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/petstore-api",
            "args": [],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build",
            "env": {
                "port": "8080",
                "mongoURI": "mongodb://root:example@localhost:27017/admin?retryWrites=true&loadBalanced=false&connectTimeoutMS=10000&authSource=admin&authMechanism=SCRAM-SHA-256"
            }
        }
    ]
}