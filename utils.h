#ifndef UTILS_H
#define UTILS_H

/**
 * @brief Duplicates the given string.
 *
 * @param s The string to duplicate.
 * @return char* A new string that is a duplicate of the given string.
 */
char* dupstr(const char* s);

#endif